import React from 'react';
import {SafeAreaView, ScrollView, Text, View} from 'react-native';

const App = () => {
    return (
        <SafeAreaView>
            <ScrollView>
                <Text style={{fontSize: 24, textAlign: 'center'}}>Sun-Voice Test</Text>
            </ScrollView>
        </SafeAreaView>
    );
};

export default App;
